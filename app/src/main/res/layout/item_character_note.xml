<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="ny.gelato.extessera.feature.character.view_model.NoteModel"/>

        <variable
            name="parent"
            type="ny.gelato.extessera.base.BaseView"/>

        <import type="android.view.View"/>
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="@{viewModel.isEmpty() ? true : false}"
        android:clipToPadding="false"
        android:foreground="?selectableItemBackground"
        android:onClick="@{(v) -> parent.onClick(v, viewModel)}"
        app:cardCornerRadius="0px">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/activity_horizontal_margin"
            android:layout_marginStart="@dimen/activity_horizontal_margin"
            android:gravity="center_vertical">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginEnd="16dp"
                android:layout_marginStart="8dp"
                android:alpha=".54"
                android:src="@drawable/ic_add_black"
                android:visibility="@{viewModel.isEmpty() ? View.VISIBLE : View.GONE}"/>

            <CheckBox
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="12dp"
                android:layout_marginStart="4dp"
                android:checked="@{viewModel.done}"
                android:onCheckedChanged="@{(cb, isChecked) -> viewModel.isEmpty() ? null : parent.onClick(cb, viewModel.toggleDone(isChecked))}"
                android:visibility="@{viewModel.isEmpty() ? View.GONE : View.VISIBLE}"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:layout_marginEnd="8dp"
                android:layout_marginStart="@dimen/activity_horizontal_margin"
                android:layout_marginTop="8dp"
                android:text="@{viewModel.text}"
                tools:text="Check the Player's Handbook to level up your character"/>

        </LinearLayout>

    </android.support.v7.widget.CardView>

</layout>